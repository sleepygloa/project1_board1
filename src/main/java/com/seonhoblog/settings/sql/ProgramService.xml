<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ProgramService">

	<select id="getProgram" resultType="hashMap">
		SELECT 
				PRO_CD
			,	PRO_NM
			,	PRO_DESC
			,	CALL_URL
			,	USE_YN
			,	IN_USER_ID
		  FROM  TB_NAMBI_PRO
		  WHERE
		  		USE_YN 	= 'Y'
		 ORDER BY PRO_CD ASC;
	</select>
	
	<insert id="insertProgram" parameterType="hashmap">
		INSERT INTO 
				TB_NAMBI_PRO 
				(
					PRO_CD
				,	PRO_NM
				,	PRO_DESC
				,	CALL_URL
				,	IN_USER_ID
		  		)
		VALUES
				(	  
				#{proCd}
			,	#{proNm}
			,	#{proDesc}
			,	#{callUrl}
			,	'sleepygloa@gmail.com'
		)
	</insert>
	

	<update id="updateProgram"  parameterType="hashmap">
		UPDATE TB_NAMBI_PRO
		   SET 
	   		PRO_NM		= #{proNm}
		,	PRO_DESC	= #{proDesc}
		,	CALL_URL	= #{callUrl}
		,	USE_YN 	  	= #{useYn}
		,	IN_USER_ID 	= #{s_userId}
		,	UP_DT 		= SYSDATE
		WHERE 
			PRO_CD 		= #{proCd}
	</update>

	
	<update id="deleteProgram" parameterType="hashmap">
		UPDATE TB_NAMBI_PRO
		   SET 
	   		USE_YN		=	'N'
		WHERE 
			PRO_CD 		= #{proCd}
	</update>


</mapper>
